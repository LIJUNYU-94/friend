// app/create-org.tsx （超適当なページ）
import { Text, View } from "react-native";

export default function ViewInvite() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>招待をチェックページ（あとで中身を作る）</Text>
    </View>
  );
}
